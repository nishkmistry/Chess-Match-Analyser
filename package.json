{
  "name": "chess-match-analyser",
  "version": "1.0.0",
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "postinstall": "cd server && npm install"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.3.1",
    "express": "^5.2.1",
    "pg": "^8.20.0",
    "react-ga4": "^3.0.1",
    "serverless-http": "^3.2.0",
    "zod": "^4.3.6"
  }
}
